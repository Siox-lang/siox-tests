module identity_b;

using identity_common::Bus;

pub trait Measure {
    fn tag() -> integer {
        return 22;
    }
}

pub trait From {
    fn hook_name() -> integer {
        return 222;
    }
}

pub struct Right(integer);
pub struct Token(integer);

impl Measure for Right {}
impl From for Right {}

pub view Endpoint for Bus {
    data in,
    ready out
}
